<ContentDialog
    x:Class="WinningEdge.Register"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinningEdge"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Navy"
    Title="Register"
    PrimaryButtonText="Register"  
    PrimaryButtonClick="RegisterDialog_PrimaryButtonClick">
    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <TextBox x:Name="rEmail" Header="Email address"/>
        <PasswordBox  x:Name="rPassword" Header="Password"/>
        <PasswordBox  x:Name="rPasswordConfirm" Header="Confirm Password"/>
        <!-- Content body -->
        <TextBlock x:Name="registerBody" Style="{StaticResource MessageDialogContentStyle}" TextWrapping="Wrap">
            <TextBlock.Text>
                Short, Sweet and Simple.
            </TextBlock.Text>
        </TextBlock>
    </StackPanel>
</ContentDialog>
