<ContentDialog
    x:Class="WinningEdge.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinningEdge"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    
    Title="Log In"
    Background="DarkViolet"
    PrimaryButtonText="Sign in"  
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="LoginDialog_PrimaryButtonClick"
    SecondaryButtonClick="LoginDialog_SecondaryButtonClick">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <TextBox Name="lEmail" Header="Email address"/>
        <PasswordBox  Name="lPassword" Header="Password"/>
        <CheckBox Name="showPassword" Content="Show password"/>
        
        <!-- Content body -->
        <TextBlock Name="body" Style="{StaticResource MessageDialogContentStyle}" TextWrapping="Wrap">
                <Run>Not signed up?</Run>
        </TextBlock>
        <Button x:Name="rButton" Content="Register Here" Click="rButton_Click"/>
    </StackPanel>
</ContentDialog>
